// Test data set - minimal data for testing
// All properties are optional in UmbMockDataSet, so we only need to export
// the data we actually want to provide. Everything else falls back to empty arrays.

export { data as user } from '../default/user.data.js';
export { data as userGroup } from '../default/user-group.data.js';
