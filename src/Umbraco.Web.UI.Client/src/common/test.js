/**
 * @ngdoc service
 * @name app.common.mongoProvider
 * @description
 * Wat
 */
angular.module('app.common')
.factory('mongoProvider', function($rootScope){
	return{

		/**
		* @ngdoc method
		* @name app.common.mongoProvider#someMethod
		* @methodOf app.common.mongoProvider
		*
		* @description
		* Shows the legacy iframe and loads in the content based on the source url
		* @param {String} source The URL to load into the iframe
		*/
		someMethod: function(source){

		}
	};
})

