/**
 * Clipboard entry value type for media picked with crop/focal-point fidelity (the Media Picker model).
 */
export const UMB_RICH_MEDIA_CLIPBOARD_ENTRY_VALUE_TYPE = 'richMedia';

/**
 * Lowest-common-denominator clipboard entry value type for a media reference without crops or focal point.
 * Enables interop between editors that pick media (e.g. Media Picker and Content Picker).
 */
export const UMB_MEDIA_CLIPBOARD_ENTRY_VALUE_TYPE = 'media';
