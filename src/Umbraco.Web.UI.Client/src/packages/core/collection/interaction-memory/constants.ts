/**
 * The unique of the interaction memory holding the alias of the last used collection view.
 */
export const UMB_COLLECTION_CURRENT_VIEW_MEMORY_UNIQUE = 'UmbCollectionCurrentView';

/**
 * The unique of the interaction memory holding the collection filter term.
 */
export const UMB_COLLECTION_FILTER_MEMORY_UNIQUE = 'UmbCollectionFilter';

/**
 * The unique of the interaction memory holding the collection ordering.
 */
export const UMB_COLLECTION_ORDER_MEMORY_UNIQUE = 'UmbCollectionOrder';

/**
 * The unique of the interaction memory holding the collection page number.
 */
export const UMB_COLLECTION_PAGINATION_MEMORY_UNIQUE = 'UmbCollectionPagination';
